# TID 监控与 Markdown 导出实机测试

2026-09-20 16:23～16:35（Asia/Shanghai）。按用户指定，由 GPT5.6-SOL、XHIGH subagent 执行，主 agent 独立核对 SQLite 与导出文件。测试使用本地已保存的有效 Cookie、真实 NGA 请求和正在运行的 Go 服务。

## 结论

两条全量任务、一次从现在开始的基线、一次有新增回复的增量，以及三份 Markdown 和一份 ZIP 导出均成功。并发配置、跨监控同时运行、限流及运行中互斥得到验证；真实 HTTP 请求未观察到重叠。全量存在一处待确认的完整性差异：47559481 的 1356 楼未入库，后续抽查已可见，现有证据不足以确定原因。

本次没有修改生产代码。自动化 fixture 已覆盖的行为不计作本次实机通过项；相关要求见 [Spec11](../spec/11-thread-page-concurrency.md) 和 [Spec13](../spec/13-markdown-rendering.md)。

## 环境与测试边界

- 实际使用用户已启动的 `service/bin/nga-reminder`，端口 8989。初始沙箱内进程/端口不可见，临时启动尝试分别因沙箱限制和端口已占用退出；没有启动第二个采集进程，也没有停止用户服务。
- 测试前无监控，三个指定 TID 均无已保存内容，因此 47288722 没有可沿用的水位。
- 新建 watch 1、2、3，分别对应 47559481、47485873、47288722。设置全天免拉取，以手动触发控制请求；三个 watch 均不绑定通知渠道。
- 测试期间临时禁用唯一飞书渠道，防止测试故障触发全局告警投递，结束后恢复原启用状态。
- 47288722 只使用 `from_now` 建立实际水位，再执行普通增量；没有重置、全量初始化、历史回填或缺口补偿，未遍历历史页面。

## 抓取结果

| TID | Run | 模式 | 页数 | 新保存条数 | 水位变化 | 耗时 |
| --- | --- | --- | --- | --- | --- | --- |
| 47288722 | 4 | 从现在开始，静默基线 | 2 | 0 | 0 → 58241 | 0.67 秒 |
| 47559481 | 5 | 全量，并发配置 8 | 68 | 1351 | 0 → 1358 | 37.15 秒 |
| 47485873 | 6 | 全量，并发配置 8 | 7 | 120 | 0 → 122 | 7.64 秒 |
| 47288722 | 7 | 普通增量 | 2 | 2 | 58241 → 58243 | 0.60 秒 |

- Run 5、6 于 16:26:55 开始，`started_at` 相差 0.149 毫秒，均以 `success`、`silent=true` 完成。47559481 实测已增长到 1358 楼；47485873 仍为用户提供的 122 楼。
- 47288722 的两轮均仅处理首页和第 2913 页。增量保存 58242、58243 楼，发布时间分别为 16:25:47、16:27:57；没有保存 58241 楼及之前的历史。运行前后均无缺口记录。
- 保存内容均为主楼/普通回复：47559481 为 1 条主楼、1350 条回复；47485873 为 1 条主楼、119 条回复；47288722 为 2 条回复。本次没有楼中楼评论样本。
- 三个 TID 的记录数与唯一内容键数一致，分别为 1351、120、2。运行中再次触发 watch 1 返回 HTTP 409，没有启动重复采集。未执行 Run 7 完成后的重复增量，因此不据此声称验证了重复增量的幂等性。
- 另有三条自动调度记录因免拉取时段而跳过，均为 0 页、0 条，不计入实际采集。

## 并发与限流

两个全量任务同时运行，小帖先完成，大帖继续处理；没有被同一个全局监控运行锁串行化。

另用生产 `NGA.ThreadPages` 对 47559481 的第 2～9 页执行只读探针，保留实际网络请求，并在 transport 开始至响应体关闭之间计数。8 个页面 worker 共耗时 3598.721 毫秒，相邻请求启动间隔为 500.076～501.026 毫秒，符合全局 120 QPM 的平滑限流。

探针测得 HTTP 在途峰值为 **1**。本次请求响应快于 500 毫秒间隔，未观察到真实请求重叠；不能将配置值 8 或等待限流的 worker 数当成实测 HTTP 并发峰值。

## 全量楼号缺口

全量后 47485873 缺 9、23、110 楼；47559481 缺 301、688、713、829、971、1068、1251、1356 楼。

针对全量主题额外只读抽查对应页面：47485873 的 1、2、6 页，以及 47559481 的 16、35、36、42、49、54、63、68 页。前 10 个楼号在复查响应中仍不存在；**1356 楼在复查时存在，但数据库中没有保存**。

没有留存首次全量第 68 页的原始响应，无法区分远端可见性变化与采集遗漏。该项标记为待确认，未通过重置或补偿追抓来掩盖差异。额外抽查均未涉及 47288722 的历史页面。

## Markdown 与 ZIP

| TID | Markdown 大小 | 导出帖子数 | 文内引用次数 | 失效文内引用 |
| --- | --- | --- | --- | --- |
| 47559481 | 527386 bytes | 1351 | 648 | 0 |
| 47485873 | 57839 bytes | 120 | 69 | 0 |
| 47288722 | 1362 bytes | 2 | 1 | 0 |

- 三份 Markdown 均返回 HTTP 200。主 agent 独立比对导出 PID 多重集合与 SQLite，完全一致；锚点唯一，718 个文内引用全部能找到目标。
- 47485873 中一条 `author_uid=-1` 的帖子保留可读匿名名。导出没有残留 `#anony_`，未发现可执行 `javascript:` 链接或原始 `<script>`。
- 图片语法数量分别为 763、132、4；47559481 有一处折叠。本次内容没有音视频和楼中楼样本，相关渲染未获得实机覆盖。
- 47559481 的 ZIP 返回 HTTP 200，大小 124148 bytes，完整性检查通过，`metadata.json.post_count=1351`，`content.md` 与独立 Markdown 字节一致。
- 本地资源下载未启用，61 条资源元数据均无本地路径；ZIP 仅包含 `content.md`、`metadata.json`，资源链接保留远程地址。本次未覆盖本地媒体文件打包。
- 每个导出入口各请求一次；没有执行重复导出或跨次哈希稳定性验证。

## 收尾与证据

三条测试 watch 已暂停，保留全天免拉取和空渠道列表；抓取内容、运行记录及水位保留在本地 SQLite。如后续要启用自动监控，还需移除测试用全天免拉取时段并按需配置通知渠道。

唯一飞书渠道已恢复启用，系统开放告警、全部投递和待投递均为 0。用户原服务保持运行，结束时 `/healthz` 返回 200。

详细证据、响应头、脱敏汇总与实际 Markdown/ZIP 位于 Git 忽略目录 `.local/live-tid-test-20260920/`，索引为其中的 `README.md`；真实内容和凭据不进入版本控制。`primary-export-verification.json` 保存主 agent 的独立导出核验，`concurrency-probe.txt` 和 `missing-floor-probe.txt` 保存远端探针结果。
